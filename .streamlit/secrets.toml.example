# Streamlit Secrets 範本
# 在 Streamlit Cloud 部署時，將此內容貼到 App settings > Secrets

# GitHub Personal Access Token（必需）
GITHUB_TOKEN = "ghp_your_token_here"

# Google Gemini API Key（推薦在雲端使用）
GEMINI_API_KEY = "your_gemini_key_here"

# Embedding 模式（雲端建議使用 gemini，避免每次重新下載模型）
EMBEDDING_METHOD = "gemini"
